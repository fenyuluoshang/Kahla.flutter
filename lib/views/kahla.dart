import 'package:flutter/material.dart';

class KahlaView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Material(
      child: Center(
        child: Text("kahla page test"),
      ),
      color: Colors.blueGrey[50],
    );
  }
}
