import 'package:flutter/material.dart';

class FriendView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Material(
      child: Center(
        child: Text("friend page test"),
      ),
      color: Colors.blueGrey[50],
    );
  }
}
